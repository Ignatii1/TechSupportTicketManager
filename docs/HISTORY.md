# HISTORY.md — past rounds, newest first

Why things are the way they are. **Not needed for most tasks** — `PROGRESS.md` has the current state, git has the diffs.
When you finish a round, add a short entry at the top: date, what changed and why, what was verified and how, what wasn't.

### 2026-09-26 — v0.8.0: исполнители в панели, новые комментарии
- Пользователь работает с 0.7.0 каждый день и попросил в панели «Исполнители» — кто ещё на заявке. Сделано вместе с
  «Инициатором» и группой исполнителей (API-IDEAS 2.3, те же ответы): поля с сервера, хранятся на карточке, освежаются
  каждой синхронизацией (`Apply`); поля нет в ответе — остаётся прежнее, json null — «пусто». Старые карточки
  дочитываются тихо при первом открытии. Попали и в ответ `TB ticket` для Claude.
- Следующей фичей выбраны **новые комментарии**: для техподдержки главный вопрос дня — «мне ответили?». Сигнал —
  `Changed` заявки в том же списке «мои открытые»: сдвинулся — перечитываем её переписку (до 10 за заход, по кругу
  попыток); без него пришлось бы читать переписку всех карточек каждые 5 минут. Счёт — `AutoSyncRules.Unread`
  (самопроверка): чужие комментарии новее «видел до»; свой ответ сдвигает «видел до»; свои узнаём по `EditorId`, без
  него — по имени из getcurrentuserinfo. Все даты — сервера, часы ПК не участвуют. Первая встреча с карточкой — отсчёт
  от её `Changed` (+1 с: он бывает грубее дат комментариев), без запроса и без потока «новых» из старой переписки.
- «Прочитано» — только когда переписка показана в открытой панели активного окна доски: окно в трее, свёрнуто или за
  браузером — значок остаётся. Уведомление общее с новыми и закрытыми (одно на заход); щелчок выбирает заявку.
- Ревью (`/code-review high`), 10 находок, исправлены все: бейдж растягивался последним ребёнком DockPanel; «видна» было
  `IsVisible` (правда и за другими окнами) — стало «активно»; сбойные заявки загораживали очередь переписки — очередь по
  попыткам; отсчёт от `Changed` с запасом в секунду; нет уведомления о комментарии, который читают прямо сейчас; при
  смене настроек посреди проверки состояние не пишется (иначе уведомление терялось бы); `TB ticket` берёт с карточки
  то, чего нет в ответе; `Creator` в списке разбирается как в заявке; дочитывание при открытии — тихое.
- Второе ревью (по исправлениям), 10 находок; главное — пересмотрено решение первого: «не уведомлять о комментарии,
  который читают» опиралось на «окно активно», а активное окно не значит, что человек у экрана (ушёл, экран заблокирован)
  — ответ пропадал бы без уведомления и без значка. Теперь уведомление — всегда, а автообновление никогда не считает
  прочитанным само: новое появляется в открытой переписке, значок гасит действие человека (выбор карточки, панель, ⟳,
  активация окна, щелчок или клавиша на доске). Ещё: отсчёт +1 с — в `AutoSyncRules.SeenFrom` под самопроверкой; без
  API карточка не записывается в «уже дочитывали»; тихое дочитывание и очередь переписки не ходят со старым клиентом;
  `TB ticket` берёт людей целиком из ответа или целиком с карточки — и тогда так и пишет. Оставлено: «PROGRESS говорит
  о выпуске до выпуска» — выпуск в этом же раунде.
- Проверено: сборка, SelfCheck (новые поля во всех формах ответа, `EditorId`, правило новых комментариев, строка
  `TB ticket`). **Не проверено:** поля на живом сервере (есть ли `Executors`/`Changed`/`EditorId` и в каком виде) и весь
  UI на Windows.

### 2026-09-24 — v0.7.0: автообновление
- Пользователь проверил 0.6.0 на живом сервере: импорт и F5 работают. Следующее по дорожной карте (API-IDEAS 1.2) —
  чтобы доска жила сама: раз в N минут (5 по умолчанию) тихо делаем импорт + F5 без окон.
- Не `ChangedMoreThan`, а тот же список «мои открытые», что у импорта: он уже проверен на живом сервере, а у
  `ChangedMoreThan` не проверены ни формат даты, ни часовой пояс сервера. При объёме пользователя — 1–2 запроса за раз.
  Карточки, выпавшие из списка (закрыты, переданы, чужие), перечитываются по одной, до 20 за раз, по кругу.
- Что добавляется: только новые назначения. Первый запуск запоминает открытое, чего нет на доске, — его приносит
  импорт; удалённая с доски карточка не возвращается, пока заявка открыта (`AutoSyncSkipIds` в settings.json, правило —
  `AutoSyncRules` с самопроверкой; неполный список ничего не «забывает»). Импорт и быстрое добавление возвращают.
- Закрытые сам не переносим (решение 0.4.0: без согласия — никогда): уведомление, по щелчку — вопрос F5
  (`AskMoveClosed`, теперь общий). Уведомление — когда статус стал закрытым, а не пока он закрыт. Итог — в заголовке
  доски, ошибки — в лог по одной, а не каждые 5 минут.
- Импорт и F5 разобраны на общие части (`FetchMyOpenAsync`, `NewCard`, `ClosedToMove`, `AskMoveClosed`) — поведение то же.
- Ревью (`/code-review high`), 10 находок, исправлены все: по неполному списку (ошибка, потолок страниц) пропуски не
  сохраняются и первый запуск ничего не решает; перечитывание по кругу, а не по LastSyncAt (сбойная заявка вечно
  стояла первой), сбой — в лог раз на заявку; о закрытой — по переходу статуса, и после перезапуска не повторяется;
  импорт пишет settings.json раз, а не на каждую заявку; `_myId` не запоминается от сменённого клиента;
  F5 и автообновление перечитывают общим `RecheckAsync`; уведомление режется до 63/250 символов
  (H.NotifyIcon бросал на длинном) и не роняет таймер; settings.json пишется через временный файл.
- Второе ревью (по исправлениям), 10 находок; сделано: конец списка — по неполной странице (без Paginator сервер
  Total не знает), целый — ещё и если различных номеров не меньше Total (список сортирован по изменению и сдвигается,
  пока листаем); справочник статусов — каждый раз, без кэша (новый статус выбросил бы заявки из списка); смена адреса или
  логина сбрасывает пропуски; ответ от сменённого клиента не применяется; идёт F5 — уведомления о закрытых нет;
  в лог — ошибка каждой новой сбойной заявки; LastSyncAt сохраняется, хоть и меняется каждые 5 минут (правило AGENTS:
  сохраняемое не в «косметике»). Пропущенное уведомление о закрытой не повторяем — вместо этого счётчик в заголовке доски.
  Попутно найдено: битый settings.json затирался дефолтами сразу при запуске (комментарий в коде говорил обратное) —
  теперь уходит в `.corrupt-…` с уведомлением; запятая в конце и `//`-комментарии не порча; занятый файл читаем
  с повтором, не прочитали — дефолты без записи и уведомление.
- Третье ревью (по вторым исправлениям), 10 находок; сделано: итог захода не применяется, если за время любого
  ожидания сменили настройки или выключили автообновление; счётчик в заголовке следует за каждой сменой статуса
  (через общий Recount); «Оставить» теперь хранит карточка (`KeptOpenStatus`, в tickets.json) — без этого счётчик
  после каждого перезапуска снова считал бы оставленные, а F5 снова о них спрашивал; в тексте о settings.json суть —
  первой (уведомление режется с конца); любой сбой разбора — дефолты, а не падение при запуске; не прочитанный
  settings.json первая запись отодвигает в `.unread-…`, а не затирает; адрес с «/» на конце и логин другими
  буквами — не новая учётная запись. Оставлено: ровно 2000 заявок на потолке страниц (ponytail); сервер, чей
  Total больше, чем он отдаёт, — на живом сервере импорт сходится; LastSyncAt сохраняется каждый заход — он виден
  в панели («обновлено …»), а запись tickets.json раз в 5 минут ничего не стоит.
- Проверено: сборка, SelfCheck (правило пропусков, листание страниц, чтение и запись settings.json).
  **Не проверено:** таймер, уведомления, щелчок по ним и счётчик в заголовке на Windows.

### 2026-09-23 — v0.6.0: Claude через буфер обмена вместо страницы с ключом API
- Пользователь: «подписка Pro, ключа API нет и не будет». Страница v0.5.0 звала Claude по ключу API — для него мертва,
  удалена целиком (сервер на 127.0.0.1, страница, собранный SDK, проверка в Chromium: −1100 строк и 190 КБ).
- С подпиской Claude живёт на claude.ai: он не может позвать компьютер пользователя, TicketBoard не может позвать его.
  Коннектор claude.ai (удалённый MCP) зовёт облако Anthropic — нужен вход из интернета во внутреннюю сеть, отвергнуто.
  Расширение поверх claude.ai — установка (политики), хрупкая вёрстка чужого сайта, а автоотправка — автоматизация
  потребительского приложения; оставлено на потом как ускоритель того же протокола. Выбрано: канал — буфер обмена,
  ход ведёт Claude. Он пишет блок `TB search/ticket/history/board`, пользователь жмёт Copy, TicketBoard
  (`ClipboardWatcher` → `ClaudeRelay`) выполняет запросы и кладёт ответ в буфер, пользователь вставляет. Ноль установки,
  ноль открытых портов, работает в любом браузере; цена — Copy и Ctrl+V на каждый круг.
- Протокол Claude узнаёт из `ClaudeRelay.Instructions` (кнопка в настройках → инструкции проекта на claude.ai). Буфер
  трогается, только если каждая строка — `TB …`; непонятный `TB` отвечается «не понял», чтобы Claude исправился.
  Ответ — текст, не JSON: короче и читается и человеком, и Claude.
- Ревью (`/code-review high`), 10 находок, исправлены все: ответ не затирает то, что скопировали, пока он собирался
  (номер содержимого буфера); блок, скопированный во время сбора, выполняется следом; сняли галочку — не пишем;
  «TB» только заглавными и нужен хоть один понятный запрос («TB total» из таблицы не трогаем); Office, файлы и
  данные менеджеров паролей не читаем (по списку форматов, до чтения); чтение буфера — под try; `TB ticket` без API
  берёт описание и ссылку с карточки; `TB board` следует переключателю «скрывать старое Готово»; помощники буфера —
  общие (заодно «Скопировать ссылку» больше не падает на занятом буфере).
- Проверено: сборка, SelfCheck — разбор блоков и живой прогон `RunAsync` с настоящим клиентом против поддельного
  Интрасервиса на loopback. **Не проверено:** слушатель буфера и окно настроек на Windows, настоящий claude.ai.

### 2026-09-22 (поздно) — v0.5.0: мост для Claude, свои окна вопросов
- **Мост для Claude.** Ограничение: интернет на рабочем ПК есть только в Brave, Интрасервис видит TicketBoard.
  Рассмотрено и отброшено: удалённый MCP-коннектор claude.ai (его вызывает облако Anthropic — нужен туннель из интернета
  во внутреннюю сеть); Claude Desktop/Code с локальным MCP (нет интернета вне Brave); расширение Brave с логином и
  паролем Интрасервиса (второе место для пароля, второй разборщик ответов на JS, нужен доступ Brave к Интрасервису и
  разрешение ставить расширения); «копировать для Claude» (Claude сам не ищет); автоматизация страницы claude.ai
  (условия использования, хрупко). Выбрано: TicketBoard отдаёт страницу чата на 127.0.0.1 и API только для чтения
  (поиск, заявка, переписка, доска — те же `HttpIntraserviceClient` и разборщики), страница в Brave зовёт Claude
  официальным SDK по ключу API пользователя. Цена — оплата API отдельно от подписки (≈5–40 ¢ за вопрос).
- Сервер — свой HTTP на `TcpListener`: поведение одинаково на Linux и Windows, поэтому проверяется здесь (у `HttpListener`
  на Windows — http.sys с резервированием URL). Защита — loopback, проверка `Host`, ключ во фрагменте ссылки (DPAPI в
  `settings.json`), CSP; ссылки в ответе кликабельны только на Интрасервис (текст заявки может «подсказать» ссылку наружу).
- Модель `claude-opus-5`, adaptive thinking, `fallbacks: "default"`, автокэш, стриминг, `eager_input_streaming` с
  проверкой входа на странице — по умолчаниям навыка claude-api. SDK 0.128.0 собран esbuild в один модуль, в exe.
- **Окна вопросов**: удаление, вопрос F5 и итоги импорта/обновления — `AskWindow` (FluentWindow на токенах приложения)
  вместо системного MessageBox; системный остался только на фатальных путях (нет доступа к папке, необработанное
  исключение). Удаление подсказывает, что в Интрасервисе заявка останется.
- Ревью (`/code-review high`), 8 находок, исправлено 7: владелец окна вопроса — не свёрнутая доска и не быстрое
  добавление; цикл приёма не крутится на постоянной ошибке сокета; повтор оборванного потока убирает показанное и
  считает токены; доска для Claude — без старого «Готово» и с обрезкой; порт 80 без порта в `Host`; одна карточка
  ищется в UI-потоке с отменой; названия приоритетов — из конвертера. Восьмая (ключи в хранилище браузера читает
  тот, кто займёт порт) — не лечится без отказа от запоминания ключей: записана как предел в README и PROGRESS.
  Проверка страницы нашла, что SDK 0.128.0 при битом JSON входа не падает, а отдаёт `{}` — это ловит проверка входа.
- Сняты 2.1 (сроки) и 2.2 (приоритеты): в компании пользователя они в Интрасервисе не заполняются.
- Ветка `claude/beautiful-albattani-pd6q7i` совпадала с `main` (ничего не потеряно), но удалить её отсюда не дали
  права — пользователь удаляет на GitHub.
- Проверено: сборка Release; SelfCheck, в том числе мост по настоящему сокету (401/403/405, ресурсы, JSON);
  `page/run.sh` — страница в headless Chromium против настоящего моста и поддельного Интрасервиса, API Anthropic
  подменён: заголовки и тело запросов, возврат thinking с подписью, параллельные инструменты, markdown, фильтр ссылок,
  XSS, ошибка ключа, обе темы, ни одной ошибки CSP. **Не проверено:** настоящий API Anthropic, Brave, окна WPF на Windows.

### 2026-09-22 (ночь) — меньше контекста на каждую задачу
- Где уходил контекст: ~20 прогонов CI по 4 минуты и несколько КБ json на каждый; ручная проверка assert в голове;
  чтение целиком файлов на 500–700 строк ради одной секции; 27 КБ `PROGRESS.md` в начале каждой сессии.
- **Локальная сборка**: `dotnet-sdk-10.0` есть в архиве Ubuntu, который прокси пропускает (закрыт был только хост
  Microsoft). Сборка за ~15 с, ловит то же, что CI, — проверено, вернув вчерашнюю ошибку с `IOException`.
- **`TicketBoard.SelfCheck`**: консоль из тех же исходников разбора — все `Debug.Assert` теперь реально выполняются
  (раньше только в Debug на Windows при старте). Провал — код 134 с текстом проверки; в Release отказывается
  запускаться, чтобы не сказать «OK» без проверок. Добавлен шагом в CI.
- **Разнесено по файлам** (`partial`, без изменения поведения): `MainViewModel` → ядро + `.Intraservice` + `.Comments`;
  `HttpIntraserviceClient` → запросы и ошибки + `.Parse` + `.SelfCheck`.
- **Документы**: `PROGRESS.md` — только состояние и открытые задачи (215 → 36 строк), журнал — сюда. `AGENTS.md`
  (грузится на каждом ходе) — сборка, SelfCheck и правила экономии контекста; архитектурные подробности — в
  `TicketBoard/README.md`. Корневой `.gitignore` — иначе `bin/obj` новой консоли попали бы в коммит.

### 2026-09-22 (ночь) — ошибки API с настоящим ответом сервера
- Просьба пользователя (он сам разработчик): не пересказывать ошибку, а показывать, что ответил сервер, — и не
  заставлять лезть в папку за `errors.log`. Лог остаётся.
- `GetAsync` раньше выбрасывал тело любого не-2xx ответа, а это ровно та часть, где сервер объясняет причину. Теперь
  сообщение — «короткая фраза (HTTP код)» и под ней тело ответа; сетевая ошибка — с цепочкой исключений («The SSL
  connection could not be established → The remote certificate is invalid…»); неразобранный ответ — с самим ответом.
  Json и xml показываются байт в байт, html-страница (IIS, прокси, страница входа) — видимым текстом без стилей и
  скриптов, с пометкой. В сообщении — первые 1000 символов, в логе — 4000, по одному образцу на метод и на код HTTP.
- Все четыре места, где показывается ошибка (синхронизация и переписка в панели, окно поиска, проверка в настройках),
  теперь выделяемые — новый стиль `SelectableText`. В окнах-сообщениях `Ctrl+C` и так копирует всё. Итог «Обновить
  статусы» называет первую ошибку, а не только их число. Окно быстрого добавления берёт только первую строку.
- Попутно: `GetAsync` ловит `IOException` (оборванное соединение посреди чтения тела) — раньше оно пролетало мимо всех
  обработчиков.
- По итогам `/code-review high` (десять находок, все приняты): **без `using System.IO` сборка бы упала** — WPF-проект
  убирает `System.IO` из неявных using (конфликт с `System.Windows.Shapes.Path`); **Basic-токен из тела ответа
  вычищается** — страница-трассировка прокси или IIS может повторить заголовок `Authorization`, а это логин и пароль
  в base64, которые иначе оказались бы на экране, в буфере обмена и в `errors.log`; щелчок по выделяемому тексту
  ошибки больше не глушит клавиши доски (поле только для чтения — не «поле ввода»); проверка подключения тоже
  показывает ответ; в html-странице заголовки и строки таблиц — с новой строки; лог — по образцу на метод и код, а не
  на код вообще; в однострочных местах — `Brief` (фраза и причина, без висящего двоеточия); в вопросе «перенести?»
  ошибки коротко, чтобы Win32 не обрезал высокое окно вместе со списком и вопросом.
- Проверено: сборка на CI (после исправлений); новые assert в `SelfCheck` посчитаны руками. Не проверено: как это
  выглядит с настоящим ответом сервера.

### 2026-09-22 (вечер) — «Обновить статусы» и неразобранные ответы в лог
- **Обновить статусы** (`F5`, трей). Импорт — половина петли: после него карточки сами не обновлялись, и закрытая днём
  заявка висела бы «В работе» до ручного «Обновить из Интрасервиса» по каждой. `RefreshAll` перечитывает все карточки
  с номером вне «Готово» (по 4 запроса разом), применяет те же правила, что и одиночная синхронизация, и спрашивает,
  перенести ли в «Готово» закрытые — закрытость по тому же правилу, что у импорта (`IsFixed`/`IsFinal` +
  `ClosedStatusNames`). Правила «что можно перезаписать» (`Apply`) и «какие статусы закрыты» (`ClosedNames`) вынесены
  в одно место, чтобы одиночная синхронизация, импорт и обновление не разъехались.
- **Неразобранный ответ сервера — в `errors.log`.** Семь вызовов написаны по документации, где у половины json не
  показан вовсе. Раньше на «непонятный ответ сервера» оставалось просить пользователя самому достать сырой json на
  рабочем ноутбуке; теперь имя метода и первые 4000 символов ответа ложатся в лог. Логина и пароля в теле нет —
  они в заголовке. Подключено через `[CallerMemberName]`, поэтому все 13 мест поменялись без правки сигнатур.
- Пока идут импорт или обновление, это видно в заголовке окна: итог приходит окном в конце, и без подсказки секунды
  ожидания выглядели как «ничего не произошло».
- **Проверено:** сборка на CI. **Не проверено:** всё на живом сервере; сколько времени занимает обновление на реальной доске.

### 2026-09-22 (вечер) — переписку видно, что она чужая
- Пользователь проверил v0.3.0 на рабочем ноутбуке: комментарии из Интрасервиса приходят и показываются правильно,
  но глазом не отличаются от собственных заметок. Так и было: строка переписки заливалась `SubtleFill` — это 4%
  чёрного, — а рельс слева рисовался `ControlStrokeBottom`, 16% чёрного в 2 px. Рядом с плоской заметкой — ничто.
- Теперь строка переписки: рельс 3 px цветом акцента, заливка `AccentTint`, отступ 12 px слева. Заметки остались
  плоскими и вровень с краем, так что два списка расходятся и по цвету, и по левой границе. Автор — полужирным.
- В заголовке «Заметки» приписка «не уходят в Интрасервис»: приложение теперь много общается с сервером, и стоит
  сразу снять вопрос, куда попадают заметки.
- Проверено: сборка на CI. Не проверено: как это выглядит на самом деле — нужен взгляд пользователя, в обеих темах.

### 2026-09-22 — импорт заявок, где я исполнитель (API-IDEAS 1.1)
- Трей → «Импорт моих заявок» и кнопка в панели: `GET api/user?getcurrentuserinfo=true` → `GET api/taskstatus` →
  `GET api/task?ExecutorIds=…&StatusIds=…` постранично. Всё попадает во «Входящие», только вручную, только вниз.
- «Открытая» заявка — это не только признаки `IsFixed`/`IsFinal`: пользователь считает закрытым и «Ожидание ответа
  с автозакрытием», а такой статус сервер не пометит. Поэтому признаки **плюс** список названий в новой настройке
  `ClosedStatusNames` (правится руками в `settings.json`, дефолт — четыре названия от пользователя).
- Импорт идемпотентен: заявка, которая уже на доске, только считается в «уже было» — её не трогают вовсе.
- По итогам `/code-review high` (три находки, все исправлены): пересчёт колонок и перерисовка значка в трее теперь
  склеиваются (`QueueRecount` / `QueueTrayUpdate`) — раньше каждая добавленная заявка перерисовывала значок целиком,
  и импорт на 300 заявок подвесил бы UI; упёршийся потолок страниц больше не выглядит как полный импорт.
- Найдено попутно и записано в `AGENTS.md`: в WPF-UI 4.3.0 элементы `SymbolRegular` со значением выше `0xFFFF`
  (например `ArrowImport16` = `0xF0384`) не рисуются вообще — библиотека режет такой код при преобразовании в строку.
  Кнопка была бы пустой, и никакой ошибки. Взяли `ArrowDownload16`, глиф проверен прямо в шрифте.
- **Проверено:** сборка Release на Windows CI после каждого шага. **Не проверено:** ничего на живом сервере и ни один
  пиксель — ни `getcurrentuserinfo`, ни `api/taskstatus` в документации вообще не показаны в json, только xml.

### 2026-09-21 (вечер) — переписка из Интрасервиса и поиск на сервере
- **Переписка в панели.** `GET /api/tasklifetime` — комментарии и смены статуса по заявке, своя секция под «Заметками».
  Не один общий список с заметками намеренно: API отдаёт даты в часовом поясе пользователя Интрасервиса, а заметки
  живут в поясе компьютера — вперемешку они молча сортировались бы неправильно. Записи без комментария (большинство)
  спрятаны за кнопкой-глазом. Нигде не сохраняется: в памяти плюс кэш на 2 минуты, так что `tickets.json` от кликов по
  карточкам не переписывается и чужие имена с внутренними текстами на диск не попадают.
- **Поиск на сервере.** `Enter` в поле поиска → `GET /api/task?search=…`, который ищет и по полям заявки, и по тексту
  всех комментариев. Результаты в отдельном окне: ↗ в браузер, «+ На доску» через тот же путь, что и быстрое
  добавление. Заявка, уже лежащая на доске, погашена.
- Попутно исправлено: проверка фокуса поля поиска (`Keyboard.FocusedElement` → `IsKeyboardFocusWithin`, `ui:TextBox`
  составной — старая проверка, похоже, не срабатывала никогда, `Esc` в поиске тоже её использовал).
- **Как делалось:** разбор документации и план — отдельным агентом-планировщиком, три шага реализации — тремя агентами
  с непересекающимися наборами файлов (клиент → панель ‖ поиск), затем `/code-review high` по всему диапазону:
  четыре находки, три исправлены (дубль карточки при повторном добавлении, пустое окно на коротком запросе, вечный кэш
  переписки), четвёртая — эта самая документация.
- **Проверено:** сборка Release на Windows CI после каждого шага. Разбор дат прогнан вручную: формат из документации
  (`12.11.2015`) разбирается раньше общего, иначе инвариантная культура читает его как 11 декабря.
- **Не проверено:** всё, что касается живого сервера и любой UI. Форма json для `tasklifetime` в документации
  отсутствует (там xml), поэтому оба разборщика терпят три вида обёрток и пропускают негодные строки вместо падения.

### 2026-09-21 — семь замечаний после первого запуска на Windows
- **Заметки удаляются** (✕ в строке, появляется по наведению) и **приоритет меняется клавишами 1/2/3 на доске** — это было
  сделано ещё 13-го, но лежало в неслитой ветке, поэтому в сборке из `main` не работало. Ничего не переделывалось.
- **Данные переехали к exe** (`App.DataDir` = `AppContext.BaseDirectory`): папка с программой копируется целиком на другой
  ПК или флешку. Выбран этот вариант, а не `datadir.txt`, потому что данных у пользователя ещё нет и переносить нечего.
  Цена — exe должен лежать там, куда есть запись; при старте это проверяется и выдаётся одно понятное сообщение вместо
  падения сохранения раз в полсекунды. Пароль по-прежнему не переносится (DPAPI, пользователь+машина) — описано в README.
- **Голый номер заявки** в быстром добавлении: `702180` без решётки распознаётся, если в поле нет ничего кроме цифр
  (4–8). Регулярка из настроек не трогалась, иначе число в тексте («картридж 12345») стало бы номером. Ссылка для такой
  заявки собирается из базового адреса. Добавлена `IntraserviceLinkParser.SelfCheck`.
- **Цифры 1/2/3 в быстром добавлении теперь работают только при распознанной ссылке.** Раньше — «поле пустое или есть
  номер», и с вводом голого номера первая цифра номера уходила бы в приоритет: `123456` превращалось в `456`.
- **Вёрстка:** дата и текст заметки на одной базовой линии (`BlockLineHeight`); значение «Статуса» по центру (убрана
  жёсткая высота 28, которая ломала шаблон WPF-UI); окно быстрого добавления подгоняется по содержимому
  (`SizeToContent`) — раньше футер накрывал кнопки приоритета.
- **Проверено:** сборка Release на Windows CI после каждого коммита (последняя — run `35627811908`); `/code-review high`
  по всему диапазону, шесть находок, пять исправлено (шестая — миграция данных из `%APPDATA%` — отклонена: данных нет);
  разбор ссылок дополнительно прогнан на эквивалентной модели регулярок вне C#.
- **Не проверено:** ничего из UI. Нужен прогон по списку в «Open work» на Windows.

### 2026-09-13 — приоритет с клавиатуры, удаление заметок
- `1` / `2` / `3` на доске меняют приоритет выбранной заявки (`MainWindow.OnPreviewKeyDown` → `MainViewModel.SetSelectedPriority`).
  Раньше эти клавиши работали только в окне быстрого добавления, на доске не делали ничего.
- Крестик в строке заметки (виден при наведении на строку) удаляет её: `MainViewModel.DeleteNote`, стиль `NoteDeleteButton`.
  Без подтверждения — откат через ежедневный бэкап `tickets.json`.
- Проверено: сборка `dotnet publish -c Release` на CI (Windows) — зелёная (локально собрать не вышло: в контейнере агента нет
  .NET SDK, загрузка закрыта сетевой политикой). Поведение в UI не проверялось — нужен запуск на Windows.

### 2026-09-13 — agent docs
- Added `AGENTS.md` (code map, "change X → file" table, gotchas) and this `PROGRESS.md`. `CLAUDE.md` imports `AGENTS.md`.
- The todo checklist moved here from `TicketBoard/README.md`, so there's a single list.
- No code changes.

### 2026-09-12 — v0.1.0: CI, packaging, docs (`65fc377`, `3133130`, `5e79953`)
- GitHub Actions on `windows-latest`: exe artifact on each push/PR, Release on `v*` tags. Actions bumped off Node 20.
- Dropped the redundant `System.Security.Cryptography.ProtectedData` package (it ships with Windows Desktop and caused NU1510).
- The single-file exe is compressed (156 → 67 MB).
- Default hotkey changed to `Ctrl+Alt+Space`, because `Win+Shift+Space` is the Windows input-language switch.
- `Ctrl+Del` delete wired up (it was documented but never worked).
- Unobserved task exceptions are now shown. All errors are appended to `errors.log`.
- Docs split: root `README.md` is the user guide, `TicketBoard/README.md` holds the developer notes.

### 2026-09-12 — refactor (`33980d6`)
- Removed `IIntraserviceClient` and `NullIntraserviceClient`; a nullable `HttpIntraserviceClient?` is used instead (`null` = not configured).
- Removed the unused `TicketRules.HideDoneDays`.
- Added the Debug-only `SelfCheck` for `Parse`.

### 2026-09-12 — Intraservice API + settings window (`281769a`, `62ae2a9`, merge `01c4050`)
- `HttpIntraserviceClient`: Basic auth, `GET api/task/{id}?include=status`, 10 s timeout, errors mapped to short messages.
- Settings: login plus a DPAPI-encrypted password. The old `IntraserviceApiToken` was dropped (old files still load). Warning on http.
- Quick capture shows the ticket title (debounced). New tickets get title, description and status. Context menu «Обновить из Интрасервиса».
- `SettingsWindow` with inline validation and «Проверить подключение». Changes apply live on save.
- Merge fixes: the tray overload dot redraws when the limit changes; a stale lookup result is ignored.

### 2026-09-12 — tray badge (`a201c25`, merge `c8193e2`)
- The tray icon is rendered at runtime: a theme glyph, an Inbox count badge ("9+" cap) and a WIP overload dot. Counts ignore filters.
- The `System.Drawing.Icon` is built from a stream so it owns its HICON. H.NotifyIcon's `GeneratedIconSource` leaks handles.

### 2026-09-12 — card appear animation (`f1911c0`)
- 200 ms fade and slide on move or add, driven by an in-memory timestamp consumed once in `OnCardLoaded`.

### 2026-09-12 — initial import (`3d74d26` … `9ac0d5d`)
- Design handoff bundle from Claude Design («Трекер заявок»). It's removed from the tree but still in history: `git show 579b8b9 --name-only`.
- WPF sources unpacked into `TicketBoard/`.
